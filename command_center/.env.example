# ═══════════════════════════════════════════════════════════════
# Arden // Command Center — Environment Configuration
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════════

# ── Server ───────────────────────────────────────────────────
# Host and port for the FastAPI server
HOST=0.0.0.0
PORT=3000

# ── Paths ────────────────────────────────────────────────────
# Workspace root directory (WSL2 path)
WORKSPACE_DIR=/home/mikegg/.openclaw/workspace

# Avatar images directory
AVATARS_DIR=/home/mikegg/.openclaw/workspace/avatars

# File upload destination
UPLOADS_DIR=/home/mikegg/.openclaw/workspace/imports/uploaded-docs

# SQLite database path
DB_PATH=/home/mikegg/.openclaw/workspace/command_center.db

# Quick launch button config
QUICK_LAUNCH_JSON=/home/mikegg/.openclaw/workspace/quick_launch.json

# ── Budget ───────────────────────────────────────────────────
# Monthly API spend limit in USD
MONTHLY_BUDGET=60.0

# Daily API spend soft limit in USD (triggers alert)
DAILY_BUDGET=5.0

# ── Local AI ─────────────────────────────────────────────────
# LM Studio API endpoint (set to empty to disable)
LM_STUDIO_URL=http://localhost:1234

# ── Logging ──────────────────────────────────────────────────
# Log level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# ── API Keys (for provider status validation — optional) ─────
# These are NOT used to make API calls, only to verify key presence
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# OPENROUTER_API_KEY=sk-or-...
